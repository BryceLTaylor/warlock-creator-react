import React from 'react';
import './Warlock.css';

const Warlock = (props) => (
    <div
        className = 'warlockInfo'
        id = 'warlockInfo'
    >
        Info for the gegnerated warlock
    </div>
);

export default Warlock;
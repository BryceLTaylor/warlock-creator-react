import React from 'react';
import './InfoPanel.css';

const InfoPanel = (props) => (
    <div
        className = 'infoPanel'
        id = 'infoPanel'
    > 
        The Info Panel
    </div>
)

export default InfoPanel;
